@*@{
        ViewBag.Title = "ShoppingCart";
        Layout = "~/Views/Shared/_Layout.cshtml";
    }*@

<div class="columns-container">
    <div class="container" id="columns">
        <!--page heading-->
        <h2 class="page-heading">
            <span class="page-heading-title">Giỏ hàng</span>
        </h2>
        <!--../page heading-->
        <div class="page-content page-order">

            <div class="heading-counter warning">
                <span class="fa fa-shopping-cart"></span> Giỏ hàng của bạn gồm:
                <span id="count-item-cart" style="font-weight: bold">1</span> <span> sản phẩm</span>
            </div>
            <div class="order-detail-content">
                <table class="table table-bordered table-responsive cart_summary">
                    <thead>
                        <tr>
                            <th class="cart_product">Ảnh</th>
                            <th>Tên sản phẩm</th>
                            <th>Đơn giá</th>
                            <th>Số lượng</th>
                            <th>Tổng tiền</th>
                            <th class="action"><i class="fa fa-trash-o"></i></th>
                        </tr>
                    </thead>
                    <tbody id="show-item-cart">
                        @*@foreach (var item in Model)
                            {
                                <tr>
                                    <th class="cart_product"><img src="~/Content/images/product-detail/@item.Anh" /></th>
                                    <th>@item.TenSP</th>
                                    <th>@string.Format("{0:#,##0}", item.Gia)</th>
                                    <th>@item.SL</th>
                                    <th>@string.Format("{0:#,##0}", (item.SL * item.Gia))</th>
                                    <th class="action"><i class="fa fa-trash-o"></i></th>
                                </tr>
                            }*@
                    </tbody>
                    <tfoot>
                        <tr>
                            <td colspan="2" rowspan="1"></td>
                            <td colspan="2"><strong>Thành tiền</strong></td>
                            <td colspan="1" id="total-item-price" style="font-weight: bold; color: #e84d1c; font-size: 20px;">54,000</td>
                            <td></td>
                        </tr>
                    </tfoot>
                </table>
                <div class="cart_navigation">
                    <a class="prev-btn" href="/Home/Index">Tiếp tục mua sắm</a>
                    <a class="next-btn" href="/ThanhToan">Thanh toán</a>
                </div>
            </div>
        </div>
    </div>
</div>
